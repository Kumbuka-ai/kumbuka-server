<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>ai.kumbuka</groupId>
        <artifactId>kumbuka-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>kumbuka-server</artifactId>
    <packaging>jar</packaging>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Pin during scaffold review: -->
        <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
        <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
        <quarkus.platform.version>3.33.2</quarkus.platform.version>

        <!-- Quarkiverse MCP server. The `quarkus-mcp-server-http` artifact
             includes both legacy HTTP/SSE and modern Streamable HTTP
             transports (see ADR-0004). 1.13.0 targets Quarkus 3.33.2. -->
        <quarkus-mcp.version>1.13.0</quarkus-mcp.version>

        <surefire-plugin.version>3.5.0</surefire-plugin.version>
        <failsafe-plugin.version>3.5.0</failsafe-plugin.version>
        <jacoco-plugin.version>0.8.12</jacoco-plugin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>${quarkus.platform.artifact-id}</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Frozen tenancy SPI (ADR-0011). v1.0.0 contract. -->
        <dependency>
            <groupId>ai.kumbuka</groupId>
            <artifactId>kumbuka-spi</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Core -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-arc</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest-jackson</artifactId>
        </dependency>

        <!-- OIDC: two tenants (mcp = bearer, admin = web-app BFF) -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-oidc</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-keycloak-admin-rest-client</artifactId>
        </dependency>

        <!-- Persistence -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-hibernate-orm-panache</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-jdbc-postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-flyway</artifactId>
        </dependency>

        <!-- MCP server (Streamable HTTP) -->
        <dependency>
            <groupId>io.quarkiverse.mcp</groupId>
            <artifactId>quarkus-mcp-server-http</artifactId>
            <version>${quarkus-mcp.version}</version>
        </dependency>

        <!-- Vert.x route filters — needed for the X-Kumbuka-Version header
             on non-JAX-RS surfaces (/mcp, served by quarkus-mcp-server-http
             via raw Vert.x routes; the JAX-RS Provider filter doesn't see them).
             Artifact `quarkus-reactive-routes` (formerly quarkus-vertx-web)
             ships the @io.quarkus.vertx.web.RouteFilter annotation. -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-reactive-routes</artifactId>
        </dependency>

        <!-- Observability -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-smallrye-health</artifactId>
        </dependency>

        <!-- Scheduled jobs (scope_stats refresher; see ADR-0014). -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-scheduler</artifactId>
        </dependency>

        <!-- Tests -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-test-security</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit5-mockito</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.26.3</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-test-keycloak-server</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <version>1.20.4</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>1.20.4</version>
            <scope>test</scope>
        </dependency>
        <!-- Instruments @QuarkusTest-driven code for JaCoCo (the Quarkus test
             classloader otherwise hides coverage). Writes target/jacoco-quarkus.exec,
             which the jacoco-maven-plugin's report goal merges with target/jacoco.exec. -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-jacoco</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <version>${quarkus.platform.version}</version>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                            <goal>generate-code</goal>
                            <goal>generate-code-tests</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!--
                Bake a Jandex index into the published JAR. The commercial
                SaaS runtime (ops-console/saas-runtime/) consumes this
                module as a regular Maven dependency and lets its own
                Quarkus augmentation discover beans here. Without a
                pre-built index, the consumer would need an explicit
                `quarkus.index-dependency.kumbuka-server.*` block to scan
                this jar. Cheap to generate, and it makes the library
                self-describing for ArC and Hibernate-ORM. See D-OPS-23.
            -->
            <plugin>
                <groupId>io.smallrye</groupId>
                <artifactId>jandex-maven-plugin</artifactId>
                <version>3.2.2</version>
                <executions>
                    <execution>
                        <id>make-index</id>
                        <goals><goal>jandex</goal></goals>
                    </execution>
                </executions>
            </plugin>
            <!--
                Source jar — published to GitHub Packages so consumers of
                the kumbuka-server library can step into the OSS sources.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals><goal>jar-no-fork</goal></goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire-plugin.version}</version>
                <configuration>
                    <systemPropertyVariables>
                        <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>${failsafe-plugin.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <systemPropertyVariables>
                        <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <!--
                Coverage. Quarkus-only project (every test class is
                @QuarkusTest), so the standard surefire-attached agent
                cannot instrument the Quarkus classloader's classes.
                The quarkus-jacoco extension does that work and writes
                target/jacoco-quarkus.exec. We therefore deliberately
                DO NOT attach the agent via prepare-agent — per the
                Quarkus docs this causes an empty jacoco.exec to race
                with jacoco-quarkus.exec and produce a 0%-coverage
                report. We only run the `report` goal here against
                the file Quarkus's extension produces.
            -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco-plugin.version}</version>
                <executions>
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <dataFile>${project.build.directory}/jacoco-quarkus.exec</dataFile>
                            <outputDirectory>${project.build.directory}/site/jacoco</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
            Mutation testing (anti-slop quality gate). Isolated in a profile so
            the normal build/CI lifecycle is untouched; invoked explicitly via
            `mvn -Pmutation org.pitest:pitest-maven:mutationCoverage`.

            CAVEAT (empirically established): PIT mutates bytecode and runs the
            tests in forked JVMs to attribute kills. @QuarkusTest classes run
            under the Quarkus test classloader (the same reason this module uses
            quarkus-jacoco instead of jacoco prepare-agent, see above), which
            PIT cannot match to mutated classes — so mutation coverage is only
            meaningful for the PURE-Java subset (no @QuarkusTest). targetTests
            is therefore scoped to the plain-JUnit unit tests; targetClasses to
            the classes they exercise. Widening these to @QuarkusTest-covered
            code yields false 0%, not real signal.
        -->
        <profile>
            <id>mutation</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-maven</artifactId>
                        <version>1.20.2</version>
                        <dependencies>
                            <dependency>
                                <groupId>org.pitest</groupId>
                                <artifactId>pitest-junit5-plugin</artifactId>
                                <version>1.2.3</version>
                            </dependency>
                        </dependencies>
                        <configuration>
                            <!--
                                Scoped to the PURE-Java logic island exercised by
                                plain-JUnit unit tests. These are the only classes
                                PIT can attribute kills for; everything else in the
                                module is @QuarkusTest-covered and unreachable (see
                                the profile-level caveat). To widen this set, add a
                                PLAIN-JUnit (non-@QuarkusTest) unit test for the
                                target class, then list it here.
                                Baseline 2026-06-22: 7 mutations, 5 killed (71%),
                                test strength 100% (2 survivors are uncovered lines,
                                not weak assertions).
                            -->
                            <targetClasses>
                                <param>ai.kumbuka.util.MemoryKeyValidator</param>
                                <param>ai.kumbuka.tenancy.TenantBindingInterceptor</param>
                                <param>ai.kumbuka.keycloak.DefaultTenantUserScope</param>
                                <param>ai.kumbuka.admin.ProtectedEntryExceptionMapper</param>
                            </targetClasses>
                            <targetTests>
                                <param>ai.kumbuka.util.MemoryKeyValidatorTest</param>
                                <param>ai.kumbuka.tenancy.TenantBindingInterceptorTest</param>
                                <param>ai.kumbuka.keycloak.DefaultTenantUserScopeTest</param>
                                <param>ai.kumbuka.repo.ProtectedEntryExceptionMapperTest</param>
                            </targetTests>
                            <outputFormats>
                                <param>XML</param>
                                <param>HTML</param>
                            </outputFormats>
                            <timestampedReports>false</timestampedReports>
                            <!-- Phase 1 = baseline only; no mutationThreshold gate yet. -->
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>integration</id>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <configuration>
                            <includes>
                                <include>**/*IT.java</include>
                            </includes>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
